# ============================================================
# Required — stack will not start without these
# ============================================================
MONGO_ROOT_USER=changeme
MONGO_ROOT_PASS=changeme
MONGO_PATH_DATA=changeme

# ============================================================
# Optional — uncomment to override container defaults
# ============================================================
# MONGO_PORT=27017
# TZ=Europe/Berlin
