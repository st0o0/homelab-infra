version: 1
metadata:
  name: Pangolin (Cleanup)
  labels:
    blueprints.goauthentik.io/instantiate: "true"

entries:
  - model: authentik_providers_oauth2.oauth2provider
    state: absent
    identifiers:
      name: Provider for Pangolin

  - model: authentik_core.application
    state: absent
    identifiers:
      slug: pangolin
