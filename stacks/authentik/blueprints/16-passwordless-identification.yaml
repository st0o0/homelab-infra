version: 1
metadata:
  name: Passwordless Identification
  labels:
    blueprints.goauthentik.io/instantiate: "true"

entries:
  # Remove inline password from identification so the page shows
  # only the username field and a prominent passkey button.
  # The default flow already binds the password stage at order 20,
  # which renders it as a separate step automatically.
  - model: authentik_stages_identification.identificationstage
    identifiers:
      name: default-authentication-identification
    attrs:
      user_fields:
        - email
        - username
      password_stage: null
      passwordless_flow: !Find [authentik_flows.flow, [slug, passwordless-authentication]]
      webauthn_stage: !Find [authentik_stages_authenticator_validate.authenticatorvalidatestage, [name, passwordless-webauthn-validation]]
