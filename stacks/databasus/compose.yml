x-logging: &default-logging
  driver: json-file
  options:
    max-size: 10m
    max-file: "3"

services:
  databasus:
    image: databasus/databasus:v3.60.0@sha256:6d299af70377a315c4490dc78e918761124a945b22d0f1fb118f8d4b042dd167
    container_name: databasus
    restart: unless-stopped
    logging: *default-logging
    environment:
      - TZ=${TZ:-}
    ports:
      - "${DATABASUS_PORT:-4005}:4005"
    volumes:
      - ${DATABASUS_PATH_DATA:?DATABASUS_PATH_DATA is required}:/databasus-data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - db-net
    healthcheck:
      test: ["CMD", "databasus", "healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 512M
        reservations:
          memory: 64M

networks:
  db-net:
    external: true
