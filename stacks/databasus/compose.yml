x-logging: &default-logging
  driver: json-file
  options:
    max-size: 10m
    max-file: "3"

services:
  databasus:
    image: databasus/databasus:v3.58.0@sha256:5457910e2929c593e654661a0e32fcee821405ba8b36eb9bddaa01c0c4f0a422
    container_name: databasus
    restart: unless-stopped
    logging: *default-logging
    environment:
      - TZ=${TZ:-}
    ports:
      - "${DATABASUS_PORT:-4005}:4005"
    volumes:
      - ${DATABASUS_PATH_DATA:?DATABASUS_PATH_DATA is required}:/databasus-data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - db-net
    healthcheck:
      test: ["CMD", "databasus", "healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 512M
        reservations:
          memory: 64M

networks:
  db-net:
    external: true
