{$CADDY_DOMAIN_PAPERLESS} {
	import defaults
	import ratelimit-default paperless

	reverse_proxy {$CADDY_TARGET_PAPERLESS} {
		header_up X-Real-IP {remote_host}
		header_up X-Forwarded-For {remote_host}
	}
}
