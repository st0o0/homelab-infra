{$CADDY_DOMAIN_SEERR} {
	import defaults
	import ratelimit-default jellyseerr
	import access-geo

	route {
		import authentik

		reverse_proxy {$CADDY_TARGET_SEERR} {
			header_up X-Real-IP {remote_host}
		}
	}
}
