type: trigger
name: custom/honeypot-trap
description: Instant ban on honeypot path access
filter: "evt.Meta.log_type == 'honeypot'"
groupby: evt.Meta.source_ip
blackhole: 1m
labels:
  type: honeypot
  remediation: true
