type: leaky
name: custom/waf-attack
description: Ban IPs that repeatedly trigger WAF rules
filter: "evt.Meta.log_type == 'waf'"
groupby: evt.Meta.source_ip
capacity: 3
leakspeed: 20s
blackhole: 1m
labels:
  type: waf
  remediation: true
