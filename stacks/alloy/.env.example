# Required
HOST_HOSTNAME=changeme
REMOTE_WRITE_URL=changeme
LOGS_PUSH_URL=changeme

# Optional
# ALLOY_CONTAINER_NAME=alloy
# CADVISOR_CONTAINER_NAME=cadvisor
# SCRAPE_INTERVAL=30s
# TZ=Europe/Berlin

# Features: copy from alloy/extras/ into alloy/ to enable
#   cp alloy/extras/nut.alloy alloy/            → UPS monitoring
#   cp alloy/extras/security-logs.alloy alloy/  → security log collection

# UPS monitor only (requires nut.alloy)
# NUT_EXPORTER_TARGET=nut-exporter:9199

# Bifrost mode: copy compose.override.bifrost.yaml -> compose.override.yaml
# Requires a running Bifrost container (deploy stacks/bifrost first)
# BIFROST_CONTAINER_NAME=bifrost
