prometheus.remote_write "central" {
  endpoint {
    url = env("REMOTE_WRITE_URL")
  }
  external_labels = {
    host = env("HOST_HOSTNAME"),
  }
}

loki.write "central" {
  endpoint {
    url = env("LOGS_PUSH_URL")
  }
  external_labels = {
    host = env("HOST_HOSTNAME"),
  }
}
