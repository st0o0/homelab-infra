services:
  alloy:
    volumes:
      - ../../alloy/features/arr.alloy:/etc/alloy/arr.alloy
    environment:
      - SONARR_EXPORTER_TARGET=${ALLOY_SONARR_EXPORTER_TARGET:-arr-sonarr-exporter:9707}
      - RADARR_EXPORTER_TARGET=${ALLOY_RADARR_EXPORTER_TARGET:-arr-radarr-exporter:9708}
      - PROWLARR_EXPORTER_TARGET=${ALLOY_PROWLARR_EXPORTER_TARGET:-arr-prowlarr-exporter:9709}
    networks:
      - default
      - arr-net

networks:
  arr-net:
    external: true
