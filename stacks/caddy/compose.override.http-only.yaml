services:
  bifrost:
    ports:
      - "8888:8888"

  caddy:
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./sites:/etc/caddy/sites:ro
      - caddy-data:/data
      - caddy-config:/config
      - caddy-logs:/var/log/caddy
