# ============================================================
# Required — stack will not start without these
# ============================================================
PIHOLE_PASS=changeme
PIHOLE_PORT_DNS=changeme
PIHOLE_PORT_WEB=changeme
PIHOLE_PATH_DATA=changeme
PIHOLE_PATH_DNSMASQ=changeme

# ============================================================
# Optional — uncomment to override defaults
# ============================================================

# Nebula Sync (leave empty to disable)
# NEBULA_PRIMARY=
# NEBULA_REPLICAS=
# NEBULA_FULL_SYNC=
# NEBULA_RUN_GRAVITY=
# NEBULA_CRON=

# TZ=Europe/Berlin
