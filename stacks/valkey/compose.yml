x-logging: &default-logging
  driver: json-file
  options:
    max-size: 10m
    max-file: "3"

services:
  valkey:
    image: docker.io/valkey/valkey:9.1.1-alpine@sha256:17539e366539969d44eb7a217237cd3ccd1a7755cdb71c7ea69be06cab2e6b9c
    container_name: valkey
    restart: always
    logging: *default-logging
    command: >-
      valkey-server
      --requirepass ${VALKEY_PASS:?VALKEY_PASS is required}
      --maxmemory ${VALKEY_MAXMEMORY:-64mb}
      --maxmemory-policy ${VALKEY_MAXMEMORY_POLICY:-noeviction}
      --appendonly yes
    ports:
      - "${VALKEY_PORT:-6379}:6379"
    volumes:
      - ${VALKEY_PATH_DATA:?VALKEY_PATH_DATA is required}:/data
    networks:
      - db-net
    healthcheck:
      test: ["CMD", "valkey-cli", "-a", "${VALKEY_PASS}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: 0.25
          memory: 128M
        reservations:
          memory: 32M

networks:
  db-net:
    external: true
