# The "bootstrap" sync is created once manually in the Komodo UI
# pointing at komodo/bootstrap/resource_syncs.toml.  Its definition
# lives here so the main sync (with delete = true) does not remove it.

[[resource_sync]]
name = "bootstrap"
description = "Bootstrap sync — loads the main resource sync definition"

[resource_sync.config]
linked_repo = "homelab-infra"
resource_path = ["komodo/bootstrap/resource_syncs.toml"]
include_resources = true
include_variables = false
include_user_groups = false
delete = false
webhook_enabled = true
pending_alert = true
