# Managed by Ansible -- do not edit manually.
COMPOSE_FILE={{ (['compose.yml']
  + (['compose.override.bifrost.yml'] if hawser_bifrost else []))
  | join(':') }}
HOST_HOSTNAME={{ hawser_hostname }}
TZ={{ hawser_tz }}

# Dockhand
DOCKHAND_SERVER_URL={{ hawser_dockhand_url }}
DOCKHAND_TOKEN={{ hawser_dockhand_token }}
AGENT_NAME={{ hawser_hostname }}
HEARTBEAT_INTERVAL={{ hawser_dockhand_heartbeat }}
