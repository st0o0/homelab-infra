galaxy_info:
  author: st0o0
  description: "Komodo Core: management server + bundled MongoDB and local periphery (SOPS/age key setup)"
  license: MIT
  min_ansible_version: "2.17"

dependencies:
  - role: docker
