# Shared feature toggles — override per host in host_vars/<host>/vars.yml

# Host toggle — set to false for hosts that are temporarily offline
host_enabled: true

# UPS monitoring (NUT client)
# ups_client_enabled: false
# ups_server_host: <IP or hostname of NUT server>
